{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the main window. The app's own storage_read/storage_write commands need no entry here — capabilities gate plugin commands, not the ones this app defines.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
