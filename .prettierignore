dist
coverage
node_modules
bun.lock
src-tauri/target
src-tauri/gen
.0*/

# Written by the plugin's build script on every cargo run, in its own shape.
# Formatting them means the generator and Prettier take turns rewriting the
# same files, and whichever ran last decides whether CI is green.
src-tauri/plugins/*/permissions/autogenerated
src-tauri/plugins/*/permissions/schemas
